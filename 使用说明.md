# Cloudflare Deploy Engine 使用说明

> **文档版本**：v0.1.3（2026-09）｜ 适用版本：Cloudflare-Deploy-Engine-v0.1.3
> **配套文档**：《README.md》（项目门面）、《使用教程.md》（快速上手）、`docs/`（插件/模板指南、ADR）
> **核心理念**：你的数据在你手里，你的选择在你手里。我们只做一件事——把你的代码送到 Cloudflare。

---

## 目录

- [1. 产品概览](#1-产品概览)
- [2. 快速开始（5 分钟）](#2-快速开始5-分钟)
- [3. 安装、启动与卸载](#3-安装启动与卸载)
- [4. 凭证管理](#4-凭证管理)
- [5. 部署详解](#5-部署详解)
- [6. AI 智能部署](#6-ai-智能部署)
- [7. 插件市场](#7-插件市场)
- [8. 数据与日志](#8-数据与日志)
- [9. 界面细节](#9-界面细节)
- [10. 故障排查](#10-故障排查)
- [11. 命令行参考](#11-命令行参考)
- [12. 安全与合规](#12-安全与合规)
- [13. 开发者与插件作者](#13-开发者与插件作者)
- [14. 附录](#14-附录)

> **文档约定**：`代码块` = 命令/文件路径；`> 提示` = 建议；`⚠️ 注意` = 风险提醒。

---

## 1. 产品概览

| 维度 | 说明 |
| :--- | :--- |
| 形态 | Windows 桌面应用（Neutralino.js 壳 + PowerShell 引擎），单目录便携 |
| 依赖 | **零安装**：仅用 Windows 10/11 自带组件（PowerShell 5.1 / curl.exe / tar.exe / WebView2） |
| 体积 | exe 2.36MB ｜ 全包 zip ≤ 1.3MB |
| 数据 | 全部在应用目录 `data/` 内；凭证 DPAPI 加密；删除文件夹 = 完全卸载（ADR-008 口径） |
| 部署目标 | Cloudflare Pages（静态站直传，官方协议）＋ Cloudflare Workers（脚本直传，可选） |
| 插件体系 | 四轴插件：sources（来源）×4 / templates（模板）×5 / ai（AI 后端）×1 / targets（目标）×2 |
| AI 角色 | 只生成方案并回填表单，**永不自动部署** |

## 2. 快速开始（5 分钟）

1. 解压 `Cloudflare-Deploy-Engine-vX.Y.Z.zip` 到任意目录（建议英文路径）。
2. 启动（二选一）：
   - 直接双击 `cloudflare-deploy-engine-win_x64.exe`；
   - 追求系统级零残留使用 `start-zeroresidue.cmd`（见 §3.4）。
3. 首启弹出免责声明 → 同意。
4. 点击「🔒 Token 权限指引」，到 Cloudflare Dashboard 创建**独立 API Token**（只勾 **Cloudflare Pages — Edit**）。
5. 填入 **Account ID / Email / API Token** → 「💾 加密保存」。
6. 部署区选「内置模板 → 纯静态示例 plain」→ 「🚀 部署」。
7. 右侧日志滚动，完成后结果卡片显示 **✅ 探针验收通过 + 站点 URL**——点击即开。

> 建议：正式部署前先点「演练 DryRun」——全程不触碰云端，验证参数与链路。

## 3. 安装、启动与卸载

### 3.1 安装
下载 Release 页 `Cloudflare-Deploy-Engine-vX.Y.Z.zip`，解压即用。**无需安装任何软件**
（Node.js / git / wrangler 都不是必需品；wrangler 仅作为可选后端，见 §5.5）。

### 3.2 SmartScreen 提示
未签名 exe 首次运行可能提示"Windows 已保护你的电脑"：
**更多信息 → 仍要运行**（ADR-005）。可核对下载页 `SHA256.txt` 哈希防止篡改。

### 3.3 WebView2 运行时
Windows 10/11 新版系统自带；缺失时按引导提示到官方下载页安装（一次性引导，不算本工具依赖）。

### 3.4 卸载与零残留
- **删除整个应用文件夹 = 完全卸载**：应用不写注册表、不写 APPDATA/USERPROFILE、不装服务。
- 唯一例外（ADR-008）：直接双击 exe 时，WebView2 运行时会写系统缓存
  `%APPDATA%\<exe名>\EBWebView`（与浏览器缓存同级，系统托管）。想连这个也收进应用目录，
  用 **`start-zeroresidue.cmd`** 启动（缓存写入应用内 `data/webview2`，删文件夹即全清）。
- 已产生的 WebView2 缓存可手动删除：`%APPDATA%\cloudflare-deploy-engine-win_x64.exe` 目录。

### 3.5 备份与迁移
- 凭证导出/导入见 §4.3；部署历史与日志随 `data/` 目录整体复制即可迁移（凭证除外）。

## 4. 凭证管理

### 4.1 加密保存
「🔑 Cloudflare 凭证」区填入 Account ID / Email / API Token →「💾 加密保存」。
- 存储位置：`data/config.enc.json`，字段值经 **Windows DPAPI（CurrentUser 作用域）** 加密；
 **绑定当前 Windows 用户**，其他账户/其他机器无法解密。
- 「👁」切换 Token 明文显示；「加载」解密回填；「清除」删除全部本地配置（凭证 + AI 设置 + 项目记忆）。

### 4.2 Token 权限指引（ADR-006）
- 建议创建**独立 API Token**（My Profile → API Tokens → Create Token），只勾选 **Cloudflare Pages — Edit**。
- 不勾选账户全局权限；选择仅限本账号的 Account Resources。
- 实测：该权限级别已可完成 Pages 全部操作，甚至可上传 Workers 脚本（§10.6 边界说明）；
  **Workers 子域启用需要更高权限**（实测 10405）。
- Token 泄露：Dashboard 立即注销重建。

### 4.3 换机迁移（ADR-003）
DPAPI 绑定当前用户，复制文件夹到新电脑**不能解密**。正确流程：
1. 旧机器：「📤 导出」→ 输入口令（≥8 字符）→ 生成 `cde-export-*.json`（PBKDF2+AES-256 加密，不含明文凭证）。
2. 新机器：「📥 导入」→ 选择文件 + 输入口令 → **在本机重新 DPAPI 加密**落盘。
3. 导出文件可安全传输；建议用后删除。

## 5. 部署详解

### 5.1 两种模式
「🚀 部署」区顶部切换：

| 模式 | 适用 | 说明 |
| :--- | :--- | :--- |
| **内置模板** | 快速体验、开箱即用 | 从 5 个预构建模板选择，填参数即部署 |
| **自定义来源** | 部署你自己的代码 | 本地文件夹 / GitHub / GitLab / 压缩包，直接上传 |

### 5.2 模板系统（5 个，全部预构建、用户零 Node）

| 模板 | 参数 | 说明 |
| :--- | :--- | :--- |
| `plain` 纯静态示例 | `site_title`、`site_tagline` | 零构建单页，第一个部署体验 |
| `astro-site` 中文技术博客 | `site_title` | 预构建中文博客页（文章×6 / 标签 / 关于 / RSS 入口），v0.1.3 起为中文本内容 |
| `react-vite` React 落地页 | `site_title`、`site_tagline`、`theme`（**下拉枚举 light/dark**） | Vite 真实构建入库，演示枚举参数 |
| `docs-site` 多页文档站 | `site_title`、`site_subtitle` | 首页 / 指南 / API 三页 |
| `nav-site` 导航站 | `site_title`、`links_json` | 粘贴 JSON 链接数组即生成卡片导航 |

`links_json` 示例（部署表单直接粘贴）：
```json
[{"name":"Cloudflare","url":"https://cloudflare.com"},{"name":"GitHub","url":"https://github.com"}]
```

参数会在部署时替换产物中的 `{{参数名}}` 占位符（文本类文件全部支持；二进制原样上传）。

### 5.3 自定义来源（4 类，全部零依赖）

| 来源 | 填什么 | 机制 |
| :--- | :--- | :--- |
| 📁 本地文件夹 | 文件夹路径（可点「浏览…」） | 目录内全部文件上传 |
| 🐙 GitHub 仓库 | `owner/repo` 或完整 URL；可选分支（默认 main） | codeload zip + tar 解压，**不需要 git** |
| 🦊 GitLab 仓库 | `group/project`（支持多级）或完整 URL；可选 ref | GitLab archive zip + tar 解压，零依赖 |
| 📦 本地压缩包 | zip / tar / tar.gz / tgz 路径 | 解压后上传（单顶层目录自动剥一层） |

> ⚠️ 文件名规则：待部署文件路径不能含空格与双引号（引擎显式校验并给出可读错误）；
> 从 GitLab 下载的仓库若含空格文件名（如某些示例仓库），部署会被规则拒绝——属预期防护。

### 5.4 项目名与域名
- 项目名决定站点域名：小写字母、数字、连字符（自动规范化）。
- **⚠️ 平台后缀**：当 `<项目名>.pages.dev` 被全局占用时，Cloudflare 会自动给子域加随机后缀
  （实测 `my-tech-blog` → `my-tech-blog-64q.pages.dev`）；**以部署结果卡片的"站点 URL"为准**
  （v0.1.3 起引擎从平台 API 获取真实子域，探针同样打在真实子域上）。
- 留空 = 自动派生（模板模式用模板 id，来源模式用目录名）。
- 项目名会记忆到配置（`settings.pagesProject`），下次部署**复用同一项目**（增量更新资产）。

### 5.5 部署后端
| 后端 | 说明 |
| :--- | :--- |
| `native`（默认） | 引擎直连 Cloudflare API 两段式资产上传，零外部依赖 |
| `wrangler` | 调用官方 wrangler CLI（需自行安装并用 `WRANGLER_PATH` 环境变量或 PATH 指向），对平台故障窗口更稳（对照参考） |

### 5.6 部署结果与探针验收
部署完成后结果卡片显示：
- **站点 URL**（真实分配子域，可点击）、项目名、后端、文件数、部署 ID；
- **探针验收**：引擎用 curl 实测**真实子域** HTTP 状态——`✅ 边缘存活（HTTP 200）` 表示站点真的活了；
  若 3 次重试后仍失败显示 `⚠️`，则属平台侧故障窗口/激活延迟（见 §10.1）；
- **部署尝试**：平台故障时自动重试次数（第 2 次起强制重传资产）。

### 5.7 部署历史与回滚（务实版）
- 每次部署（成功或失败）记入 `data/deploy-history.json`（保留最近 50 条）。
- 右侧「🗂 部署历史」：每行 = 项目 / 模板或来源 / 时间 / ✅✗ 徽标；
  - **「日志」**：打开该次部署完整日志 `data/logs/deploy-*.log`；
  - **「↻ 重部署」**：按历史记录重放（模板参数 / 来源参数 / 项目名）重新部署——
    **这就是回滚**：Pages 资产库保留历史 hash，重放旧 manifest 即恢复旧版本内容。
- 项目被销毁后历史仍保留，可重放（来源文件还在时）。

### 5.8 一键销毁
「🗑 销毁项目」→ 确认后删除整个 Pages 项目（站点下线、资产与历史部署全部消失）。
销毁同时清除配置中的项目记忆。Worker 脚本销毁见 §11.6。

## 6. AI 智能部署

### 6.1 工作原理
`需求描述 → 注入全部模板元数据的 System Prompt → 调用你的 AI Key（OpenAI 兼容协议）→
解析 JSON 方案 {template, projectName, parameters, explanation} → 回填表单（可编辑）→ 你点部署`。
**AI 没有任何部署权限，永远只回填。**

### 6.2 设置
「⚙ AI 设置」：BaseUrl / 模型 / API Key。
- 支持任何 OpenAI 兼容端点：OpenAI、DeepSeek（`https://api.deepseek.com/v1`）、Ollama 本地、vLLM 等；
- API Key 与 Cloudflare 凭证一样 DPAPI 加密存储；Key 已保存时置空 = 保持不变；
- 调用失败自动重试 1 次（2 秒间隔），仍失败则提示并可手动填写——**AI 故障不影响手动流程**。

### 6.3 示例
输入：`我想做一个个人博客，标题叫我的小站`
→ AI 可能返回模板 `astro-site`、项目名 `my-site`、参数 `{site_title:"我的小站"}`，自动回填表单；
你检查/修改后点「🚀 部署」。

## 7. 插件市场

「🧩 插件市场」模态：

| 区域 | 功能 |
| :--- | :--- |
| 市场 URL | 远程 JSON 清单地址（http/https，或本地路径）；「保存 URL」持久化到配置 |
| 搜索 | 按 id / 名称 / 描述过滤 |
| 插件列表 | 每项：名称 / 轴 / 版本 / 描述 + 「安装」按钮（已安装自动置灰） |
| 已安装区 | 第三方插件列表 + 「卸载」按钮（内置插件受白名单保护，不可卸载） |

- **安装链路**：下载（curl）→ **SHA256 校验**（与清单不符即拒绝并清理）→ 解压 → 按轴落位
  （sources/ai/targets → `scripts/plugins/<轴>/`；templates → `templates/<id>/`）→ 注册 `plugins.json`。
- 卸载 = 注销注册 + 删除文件，可逆。
- **发布侧**（开发者）：`scripts\dev\publish-plugin.ps1` 把插件目录打成 zip + 生成 registry 条目
  （含 SHA256），上传到托管后并入你的 `market.json` 即可。
- 清单协议（`market.json`）：
```json
{ "plugins": [
  { "axis": "sources", "id": "my-source", "name": "我的来源", "version": "1.0.0",
    "description": "…", "url": "https://…/sources-my-source-v1.0.0.zip", "sha256": "…" }
] }
```

## 8. 数据与日志

```
<应用目录>/
├── data/                      # 所有运行时数据（随目录删除）
│   ├── config.enc.json        # 🔒 DPAPI 加密配置（凭证/AI Key）+ 明文设置区
│   ├── deploy-history.json    # 部署历史（明文，50 条上限）
│   ├── .deploy.lock           # 部署运行锁（并发互斥，死锁 PID 探活自愈）
│   ├── logs/                  # 部署/销毁日志（deploy-*.log / destroy-*.log）
│   ├── cache/                 # 模板展开产物与 wrangler 日志（每次部署自动清理 1 天前）
│   └── webview2/              # 仅零残留启动器模式下出现（WebView2 缓存）
├── scripts/                   # 引擎（可 CLI 直用，见 §11）
├── templates/                 # 5 个预构建模板
├── plugins.json               # 插件注册表（用户可编辑）
└── 使用说明.md / README.md / 使用教程.md
```

**并发安全**：部署/销毁互斥（运行锁）；锁内进程已退出时自动接管（死锁自愈）。
若提示"已有部署任务进行中"，等待完成或结束对应 powershell.exe 进程即可。

## 9. 界面细节

| 功能 | 位置 |
| :--- | :--- |
| 语言切换（中文/English） | 顶栏右上角下拉，选择即生效并记忆 |
| 取消任务 | 顶栏「■ 取消任务」（立即终止引擎子进程；部署超 25 分钟自动终止兜底） |
| 日志区 | 右侧实时滚动，WARN/ERROR 着色 |
| 结果卡片 | 右侧中部（探针徽标/尝试次数/部署 ID） |
| 部署历史 | 右侧底部（刷新 / 日志 / 重部署） |
| 表单动态化 | 模板/来源切换即时更新参数区；枚举参数渲染为下拉 |

## 10. 故障排查

### 10.1 部署 API 成功但探针 500/000
Cloudflare Pages 存在**间歇性故障窗口/边缘激活问题**（上传返回 HTML 5xx、或部署 API 成功但边缘
长时间 500）。引擎已内置：非 JSON 响应防护 + 自动重试 3 次 + 第 2 次起强制重传 + 探针验收门。
- **v0.1.3 起探针与结果 URL 使用平台真实分配子域**（项目名冲突时平台加后缀，旧版会指向他人站点
  并产生探针假阳性——已修复）。
- 实测（2026-09-01/02，真实账号）：新部署边缘持续 500（含部署专属预览 URL），同日旧内容站点
  200；平台资产库 `check-missing` 确认上传资产**在库**——属平台边缘服务侧问题，非上传链路。
仍失败时依次尝试：
1. 等待数分钟再部署或稍后重试（激活延迟实测最短 ~60–90 秒，也有跨天窗口记录）；
2. 切换 `wrangler` 后端（官方客户端路径，对照参考）；
3. 控制台上传。

### 10.2 页面内容没更新 / 参数没生效
- 确认部署结果 `servingOk=✅`（边缘激活后才看到新内容）；
- 本地有缓存时强刷（Ctrl+F5）；
- 重部署请复用同一项目名（增量上传新资产）。

### 10.3 提示"插件已安装" / "内置插件不可卸载"
市场重装同名插件需先卸载；内置插件是基础能力，不受市场管理。

### 10.4 提示"已有部署任务进行中"
另一部署/销毁正在运行（锁 `data/.deploy.lock`）。等待完成；若确定无任务在跑，
检查是否有残留 powershell.exe 挂起，结束它后锁会自动接管清除。

### 10.5 wrangler 后端："未找到 wrangler"
安装 wrangler 并加入 PATH，或设置环境变量 `WRANGLER_PATH=C:\...\wrangler.cmd` 指向可执行文件。

### 10.6 Workers 目标权限边界（实测记录）
- `Pages:Edit` Token 实测**可以**上传/删除 Workers 脚本（PUT/DELETE 200）；
- **子域启用**（`-WorkerSubdomain`）实测返回 10405「Method not allowed for this authentication
  scheme」——需账户级 Workers 权限；此时部署成功但 workers.dev 子域不可访问（探针 000 属预期）。
- 上传 Module 格式（`export default`）需要 metadata；引擎当前按 Service Worker 格式
  （`addEventListener("fetch", …)`）直传——**请使用该格式的脚本**。

### 10.7 配置报错 / 旧版本升级
早期配置文件缺少新字段时，引擎读取自动补齐迁移（无需手工处理）。

### 10.8 其它
- 一切问题先看右侧日志与「历史 → 日志」；日志文件位于 `data/logs/`，可整份反馈。
- 平台状态参考：https://www.cloudflarestatus.com

## 11. 命令行参考（引擎 CLI，开发者/自动化）

所有脚本支持 `-ConfigPath`（默认 `<应用目录>\data\config.enc.json`）；输出协议：
`LOG|时间|级别|消息` 与 `RESULT|<json>`。

```text
部署：  deploy-core.ps1 [-ConfigPath <p>] -TemplateId plain -ParamsB64 <b64> [-Project <名>] [-Backend native|wrangler] [-DryRun]
        deploy-core.ps1 [-ConfigPath <p>] -SourceId github -SourceArgsB64 <b64> [-Project <名>]
        deploy-core.ps1 [-ConfigPath <p>] -FromHistory 0            # 0=最新，重放历史部署
        deploy-core.ps1 [-ConfigPath <p>] -ListHistory              # 历史数组
        deploy-core.ps1 [-ConfigPath <p>] -ListPlugins | -ListTemplates
        参数 JSON 一律走 Base64(UTF-8) 通道：-ParamsB64 / -SourceArgsB64（防命令行引号剥离）

销毁：  destroy.ps1 [-ConfigPath <p>] -Project <名> [-DryRun] (-Force 跳过交互确认)

配置：  config-manager.ps1 [-ConfigPath <p>] -Verb get|set|export|import|clear
        set 字段：accountId|apiToken|email|aiApiKey|aiBaseUrl|aiModel|marketUrl（值可用 -ValueB64）

AI：    ai-bridge.ps1 [-ConfigPath <p>] -RequestB64 <b64>

市场：  market.ps1 [-ConfigPath <p>] -Verb registry|search|install|uninstall
        （search -Query <词>；install -Axis <轴> -Id <id> [-Force]；uninstall -Id <id>；
         均可 -MarketUrl <清单地址>）

目标：  deploy-core.ps1 -TargetId workers -SourceId local -SourceArgsB64 <b64> -Project <脚本名>
        [-WorkerSubdomain]                                        # Workers 脚本直传

插件：  plugin-manager.ps1 -Axis <轴> -Id <id> [-ArgsB64 <b64>]   # 直接调用某个插件
```

Base64 参数生成（PowerShell 一行）：
```powershell
$b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes('{"site_title":"我的站"}'))
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy-core.ps1 `
  -ConfigPath data\config.enc.json -TemplateId plain -ParamsB64 $b64 -DryRun
```

环境变量钩子：

| 变量 | 用途 |
| :--- | :--- |
| `WRANGLER_PATH` | wrangler 可执行文件（Backend=wrangler 时） |
| `CDE_API_BASE` | 调试/取证：重定向 Cloudflare API 基址（默认 `https://api.cloudflare.com/client/v4`） |
| `CDE_DATA_DIR` | 单测/临时数据目录重定向（默认 `<应用>\data`） |
| `CDE_MARKET_URL` | 市场清单地址覆盖（优先于配置 settings.marketUrl） |

### 11.6 目标插件：Workers
见 §10.6 权限边界。引擎按 Service Worker 格式直传：
```text
deploy-core.ps1 -TargetId workers -SourceId local -SourceArgsB64 <Path 的 B64> -Project my-worker
deploy-core.ps1 -TargetId workers -FromHistory <序号>              # 重放历史（含 workers 记录）
```

## 12. 安全与合规

- **部署产物零凭证**（ADR-001）：默认不注入任何凭证进部署产物；CI 与测试有自动化断言。
- **凭证只在内存与 DPAPI 密文之间流转**：日志/导出文件/部署产物均无明文 Token（脱敏工具 + 密扫门禁）。
- **AI 永不自动部署**：方案生成与执行完全解耦。
- **合规边界**：仅用于部署"你自己的内容到你自己账号"的合法场景；本工具不含、不推销任何
  代理 / VPN / 订阅 / 优选 IP 功能（Cloudflare 条款禁止的用途不在支持范围）。
- **威胁模型提示**：DPAPI 绑定当前 Windows 用户；他人登录同一账户可解密。公用电脑请避免保存凭证。

## 13. 开发者与插件作者

| 文档 | 位置 |
| :--- | :--- |
| 插件开发指南（四轴 handler 契约） | `docs/插件开发指南.md` |
| 模板开发指南（template.json 规范 / 完整性） | `docs/模板开发指南.md` |
| 架构决策记录（ADR-001 ~ 008） | `docs/adr/` |
| 手工验收矩阵（外部验收清单） | `docs/手工验收矩阵.md` |
| 发布流水线 | `scripts\dev\build-release.ps1`（门禁：语法/Pester/密扫/模板完整性/尺寸/zip 断言） |
| 单测 | `tests\`（70 项：utils/config/plugin/template/engine-state/market/pages/workers/deploy-core） |
| GUI 端到端 | `tools\ui-e2e\`（CDP 驱动打包 exe：首启/凭证/模板/部署/历史/销毁 + 外部激活轮询） |
| 模板源码与构建 | `templates-src\`（`build-all.ps1`；react-vite/docs-site/nav-site 需 Node 构建 = 仅维护者；plain / astro-site 为手工维护预构建） |

## 14. 附录

### 14.1 理念门禁实测（v0.1.3 口径）

| 指标 | 实测 | 状态 |
| :--- | :--- | :--- |
| exe ≤ 5MB | 2.36MB | ✅ |
| templates ≤ 3MB | 0.15MB（5 模板） | ✅ |
| zip ≤ 10MB | ~1.26MB | ✅ |
| 部署内存增量 ≤ 45MB（校准口径） | 41.2MB（宿主基线 73.2MB + 峰值 114MB） | ✅ |
| 四轴插件可调用 | sources×4 / templates×5 / ai×1 / targets×2 | ✅ |
| 删除文件夹即卸载 | 应用零残留（WebView2 系统缓存按 ADR-008 豁免/可收入应用目录） | ✅ |

### 14.2 发布记录

| 版本 | 日期 | 内容 |
| :--- | :--- | :--- |
| v0.1.0 | 2026-08 ~ 09 | 引擎层（DPAPI/插件四轴/双后端/韧性）；桌面 UI 全量；AI 设置与回填；部署历史与回滚；插件市场；模板×5 + 来源×4 + 目标×2；i18n；发布流水线与尺寸门禁；真实账号端到端验证（含平台故障窗口与权限边界实测） |
| v0.1.1 | 2026-09-01 | 桌面端启动链全量修复：`Neutralino.init()` 误用（init 永不执行）、引擎目录解析（NL_PATH）、spawnProcess 参数通道（整串命令行）、RESULT 分块重组、首启遮罩死循环、静默失败可见化、stdout UTF-8 加固 |
| v0.1.2 | 2026-09-01 | 结果面板渲染作用域（装载类结果不再覆盖部署/销毁结果）；Add-DeployHistory stdout 泄漏；新增 `tools/ui-e2e` GUI 端到端测试 |
| v0.1.3 | 2026-09-01 | astro-site 模板中文化（完整中文技术博客）；站点 URL / 探针改用平台真实分配子域（修复指向他人站点与探针假阳性） |

### 14.3 已知限制与后续计划

- R2 / D1 部署目标（需新 Token 权限 + S3 签名 / SQL 迁移）；
- AI 多轮对话（当前单轮生成+回填）；
- 插件市场默认源（随项目仓库 Release 上线后配置）；
- **平台边缘激活窗口**：2026-09-01/02 观测到新 Pages 部署边缘持续 500（旧内容正常）——
  平台侧问题，引擎按设计报告；后续计划将新项目探针窗口放宽为可配置；
- 外部验收项：无 Node 干净虚拟机计时、桌面交互目测（详见 `docs/手工验收矩阵.md`，CI 已常绿）。

### 14.4 修订记录

| 日期 | 说明 |
| :--- | :--- |
| 2026-09 | 发布 v0.1.0 正式版；全文按最终实现口径校订（模板/来源/目标/CLI/权限边界实测数据） |
| 2026-09-01 | 随 v0.1.1–v0.1.3 同步：桌面端启动链修复口径、真实子域说明、模板内容中文化、测试数 70/70、平台边缘故障窗口观测补充 |